# Copy to .env and fill in to enable real AI photo->nutrition estimation.
# Without this, the app falls back to a built-in stub estimator (no key needed).
ANTHROPIC_API_KEY=
# Optional overrides
LOUIS_MODEL=claude-opus-4-8
PORT=8787
